Update the attached src/harden/artifacts.py to persist Module 2 EZ-Scan
ingestion results.

Make a minimal additive change. Preserve existing path confinement, atomic
writes, hashing, immutable artifact behavior, token exclusion, and all existing
public APIs.

Required changes:

1. Add FINDINGS_NORMALIZED = "findings_normalized" to RunPhase.

2. Import EzScanIngestionResult and NormalizedFinding from harden.contracts.

3. Add this RunDirectory method:

def record_ingestion_result(
    self,
    result: EzScanIngestionResult,
) -> tuple[StoredArtifact, StoredArtifact]:
    ...

Behavior:

- Validate that result.summary.run_id equals self.run_id.
- Write result.findings as a top-level JSON array to:
  normalized-findings.json
- Each finding must be serialized using model_dump(mode="json").
- Write result.summary to:
  ingestion-summary.json
- Both files are immutable.
- Save the files before updating manifest.json.
- Update the manifest only after both artifacts have been stored successfully.
- Set the manifest phase to RunPhase.FINDINGS_NORMALIZED.
- Add these artifact path entries:
  normalized_findings -> normalized-findings.json
  ingestion_summary -> ingestion-summary.json
- Return the two StoredArtifact objects in that order.
- Calling the method again with identical content must be idempotent.
- Calling it with different content for an existing immutable artifact must
  raise the existing ArtifactError.
- Never persist an EZ-Scan access token.

4. If useful for tests, add:

def load_normalized_findings(self) -> list[NormalizedFinding]:
    ...

Use Pydantic TypeAdapter or direct model validation so loading validates every
finding. Do not return unvalidated dictionaries.

Do not change ZIP handling or add CSV parsing to artifacts.py. CSV parsing
belongs in ez_scan_ingest.py.

If you can edit files, update artifacts.py directly. Otherwise return a unified
diff. Briefly explain the artifact lifecycle after the code.
